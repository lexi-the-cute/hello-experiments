# Notes

Cobol is a language where different compilers will treat the code differently. Usually failing to compile hello.cbl at all due to the sequence numbers at the beginning of each line. These sequence numbers aren't strictly necessary anymore, so the program will work just fine without them.
